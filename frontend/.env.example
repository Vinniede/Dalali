VITE_APP_NAME=Dalali Express Services
# Note: API_BASE_URL is auto-detected at runtime based on hostname
# For local development: http://localhost:5000/api
# For production: /api (relative path, routed by Vercel)
